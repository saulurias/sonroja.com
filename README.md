# sonroja.com

Este repositorio contiene **solo la web ya construida** de sonroja.com y existe para que GitHub Pages la sirva.

No edites nada aquí: cada push a `main` del repo privado `saulurias/sonroja` reconstruye el sitio y sobrescribe la rama `gh-pages` de este repo (workflow `publicar-web.yml`).
